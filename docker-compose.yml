services:
  {{PROJECT_NAME}}:
    build:
      context: .
      dockerfile: Dockerfile
    image: {{PROJECT_NAME}}:local
    container_name: {{PROJECT_NAME}}
    ports:
      - "8080:8080"
    env_file:
      - .env.dev
    restart: unless-stopped
